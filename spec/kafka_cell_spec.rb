require 'spec_helper'

describe KafkaCell do
  it 'has a version number' do
    expect(KafkaCell::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
